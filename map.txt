: "flic-a-disc"
- TODO: homepage w/ navigation links
- links: discogs-keepers, ebay-auctions, ebay-annotate

-> discogs-keepers
path ('pages/DiscogsKeepers.tsx')
on page load:
-> loadPage()
- setLoading(true)
-> fetch '(api/discogs/keepers')
- setListings(data.listing)
- setMlData(data.predictions, data.mean_predictions, threshold,
    uncertainties, model_version)
- setLoading(false)

-> fetchStats()
- setLabeledCount(stats.labeled)
- setTotalCount(stats.total)

-> fetch '(api/discogs/keepers) -> GetDiscogsKeepersPage -> handlers.go
- db query ("Record JOIN record on listing.record_id = record.id)
- mlRecords.append()